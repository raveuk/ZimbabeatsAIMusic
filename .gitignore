node_modules/
dist/
.next/
.expo/
web-build/

# Local data + secrets — must never be pushed.
**/.env
**/.env.local
**/.env*.local
# Only the *server*'s data dir contains the SQLite DB + JWT secret — exclude that
# specifically. web/data/ ships static config (genres.ts, news.json) that the
# Vite build needs.
server/data/
**/jwt.secret

# Editor/OS junk
.DS_Store
.vscode/
.idea/
*.log

# Dev artifacts
dev.log

// Public URL of the Next.js backend, exposed via Cloudflare Tunnel.
// The phone no longer needs to be on the same Wi-Fi — works over cellular too.
export const API_BASE = "https://api.zimbabeats.com";
